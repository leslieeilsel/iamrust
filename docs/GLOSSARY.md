# 术语表

- 用户：拥有内部 UUID 和登录凭据的账号主体。
- 好友：双方已接受的对等关系；删除后双方关系同时解除。
- 会话：消息的有序容器，分为单聊和群聊。
- 消息：带客户端幂等 ID、服务端序列号、内容和状态的会话记录。
- 群组：具有名称、成员、群主和管理员角色的多人会话。
- 设备：一次登录所在的客户端实例，可被单独撤销。
- 回执：消息已送达或已读的状态；用户可关闭对外已读回执。
- 同步游标：服务端单调递增位置，用于断线后补齐事件。
- 附件：经上传授权、校验并由消息或头像引用的对象存储文件。
- 隐身：本人保持连接，但对其他用户表现为离线。
