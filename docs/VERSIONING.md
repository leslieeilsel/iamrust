# 版本与发布规则

应用使用语义化版本。协议版本独立递增：兼容字段只追加且提供默认值；删除、改名或语义改变必须提升协议主版本。服务端至少支持当前与上一个稳定客户端协议，并通过 `server_features` 协商可选能力。

提交标题使用 Conventional Commits。每次发布更新 `CHANGELOG.md`，列出新增、修复、安全、迁移、已知问题和回滚步骤。候选版本通过三平台安装/升级冒烟后才能标记稳定版。
